node {
    stage('Clone'){
        git credentialsId: 'GIT', url: 'https://github.com/jmpcba/HC.git'
    }

    stage('Plan') {
        echo "##################"
        echo "# TERRAFORM PLAN #"
        echo "##################"
        
        dir('infra') {
            withCredentials([string(credentialsId: 'TF_VAR_access_key', variable: 'AWS_SECRET_ACCESS_KEY'), string(credentialsId: 'TF_VAR_secret_key', variable: 'AWS_ACCESS_KEY_ID'), string(credentialsId: 'TF_VAR_db_password', variable: 'TF_VAR_db_password')]) {
                ansiColor('xterm') {
                        sh "set +x"
                        sh "/usr/bin/terraform version"
                        //sh "/usr/bin/terraform init"
                        sh "/usr/bin/terraform plan"
                        
                }
            }
        }
    }
}